/** @type {import("prettier").Config} */
const config = {
  plugins: [],
  singleQuote: true,
  bracketSpacing: true,
  arrowParens: "always",
  trailingComma: "es5",
  tabWidth: 2,
  semi: true,
};

// eslint-disable-next-line no-undef
module.exports = config;
